info:
  name: TestA
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/census?limit=30&offset=10&showAlert=true
  params:
    - name: limit
      value: "30"
      type: query
    - name: offset
      value: "10"
      type: query
    - name: showAlert
      value: "true"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
