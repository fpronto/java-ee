info:
  name: CreateChar
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/api/char
  params:
    - name: comida
      value: Arroz
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
